import { Cart, CartIcon, HeaderStyle, Rocket, Search } from "./style";
import cart from "../../assets/output-onlinepngtools.png"

export function Header () {
    return (
        <HeaderStyle>
        <h1>spacetravel {/* <Rocket src={spaceship} /> */} </h1>
        </HeaderStyle>
    )
} 